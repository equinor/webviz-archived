{{ page_element(
    'FanChart',
    './test.csv',
    observations='./observations.csv',
    references='./references.csv',  
    logy=True,
    check_box=True,
    slider_columns=['category']
    ) }}
